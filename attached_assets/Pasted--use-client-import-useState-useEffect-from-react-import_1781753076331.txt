'use client'

import { useState, useEffect } from 'react'
import { Card } from '@/components/ui/card'
import { Button } from '@/components/ui/button'
import { Brain, BarChart3, Zap, Settings } from 'lucide-react'
import { LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer, BarChart, Bar } from 'recharts'

export function DashboardSidebar() {
  const [stats, setStats] = useState({
    generationsToday: 0,
    averageQuality: 0,
    topStyles: [] as string[],
  })

  useEffect(() => {
    const fetchInsights = async () => {
      try {
        const response = await fetch('/api/troxt/insights')
        if (response.ok) {
          const data = await response.json()
          setStats(data)
        }
      } catch (error) {
        console.error('[v0] Insights error:', error)
      }
    }

    fetchInsights()
  }, [])

  const chartData = [
    { name: 'Mon', generations: 4 },
    { name: 'Tue', generations: 6 },
    { name: 'Wed', generations: 8 },
    { name: 'Thu', generations: 5 },
    { name: 'Fri', generations: 9 },
    { name: 'Sat', generations: 7 },
    { name: 'Sun', generations: 6 },
  ]

  return (
    <div className="space-y-4">
      <Card className="bg-card border-primary/20 p-4">
        <div className="flex items-center gap-2 mb-4">
          <Brain className="size-4 text-primary" />
          <h3 className="text-sm font-semibold text-foreground">TROXT Status</h3>
        </div>
        <div className="space-y-3">
          <div className="flex justify-between items-center">
            <span className="text-xs text-muted-foreground">Intelligence Level</span>
            <div className="w-32 bg-muted rounded-full h-1.5">
              <div className="bg-primary h-1.5 rounded-full" style={{ width: '85%' }} />
            </div>
          </div>
          <div className="flex justify-between items-center">
            <span className="text-xs text-muted-foreground">Learning Progress</span>
            <div className="w-32 bg-muted rounded-full h-1.5">
              <div className="bg-accent h-1.5 rounded-full" style={{ width: '62%' }} />
            </div>
          </div>
          <div className="flex justify-between items-center">
            <span className="text-xs text-muted-foreground">Memory Usage</span>
            <span className="text-xs font-mono text-foreground">2.4 GB</span>
          </div>
        </div>
      </Card>

      <Card className="bg-card border-primary/20 p-4">
        <div className="flex items-center gap-2 mb-4">
          <BarChart3 className="size-4 text-primary" />
          <h3 className="text-sm font-semibold text-foreground">Today's Activity</h3>
        </div>
        <ResponsiveContainer width="100%" height={120}>
          <BarChart data={chartData}>
            <CartesianGrid strokeDasharray="3 3" stroke="rgba(0,212,255,0.1)" />
            <XAxis dataKey="name" tick={{ fontSize: 10 }} stroke="#666" />
            <YAxis tick={{ fontSize: 10 }} stroke="#666" />
            <Tooltip contentStyle={{ background: '#0a000f', border: '1px solid #00d4ff' }} />
            <Bar dataKey="generations" fill="#00d4ff" />
          </BarChart>
        </ResponsiveContainer>
      </Card>

      <Card className="bg-card border-primary/20 p-4">
        <div className="flex items-center gap-2 mb-4">
          <Zap className="size-4 text-primary" />
          <h3 className="text-sm font-semibold text-foreground">Quick Stats</h3>
        </div>
        <div className="space-y-2">
          <div className="flex justify-between text-xs">
            <span className="text-muted-foreground">Generations Today</span>
            <span className="font-mono text-foreground">{stats.generationsToday}</span>
          </div>
          <div className="flex justify-between text-xs">
            <span className="text-muted-foreground">Avg. Quality</span>
            <span className="font-mono text-foreground">{stats.averageQuality}%</span>
          </div>
          <div className="flex justify-between text-xs">
            <span className="text-muted-foreground">Models Created</span>
            <span className="font-mono text-foreground">23</span>
          </div>
        </div>
      </Card>

      <Button className="w-full gap-2" variant="outline">
        <Settings className="size-4" />
        TROXT Settings
      </Button>
    </div>
  )
}
