// ============================================================
//  EtherPrism — Composant Principal
//  Organe visuel de TroxT
// ============================================================

// TODO: Importer React quand configuré
// import React from 'react'

import type { PrismConfig, PrismState } from './types'

interface EtherPrismProps {
  config?: Partial<PrismConfig>
  onResult?: (result: unknown) => void
  onError?: (error: string) => void
}

// TODO: Implémenter le composant React
export function EtherPrism(_props: EtherPrismProps) {
  // TODO: Render UI
  return null
}

export default EtherPrism